# Workflow 編輯器 Wireframe - Part 1: 基礎架構

**版本**: 1.0.0 (Part 1/5)
**日期**: 2025-10-29
**狀態**: 🔄 進行中
**複雜度**: ⭐⭐⭐⭐⭐ (極高)

---

## 📋 頁面概覽

### 目的
提供專業級無限畫布視覺化流程編輯器，讓用戶能夠通過拖拽方式設計複雜的 Multi-Agent 工作流程，支持 12+ 種專業節點類型、實時執行追蹤、版本控制與協作。

### 目標用戶
- **Alex（IT 開發者）**: 設計複雜多步驟 Agent 工作流、調試流程執行、優化節點性能、版本管理
- **Sarah（業務分析師）**: 使用預設模板快速創建業務流程、可視化數據流、監控執行狀態
- **David（企業管理員）**: 審核工作流合規性、管理流程版本、監控資源使用、團隊協作管理

### 成功指標
- 流程設計效率提升 > 60%（相比純代碼）
- 節點配置正確率 > 90%（首次配置）
- 流程執行成功率 > 85%（無錯誤）
- 用戶學習時間 < 20 分鐘（基礎功能）
- 實時協作延遲 < 500ms
- 版本切換時間 < 2s

### 核心功能（Part 1 範圍）
✅ 無限畫布編輯器（縮放、平移、網格對齊）
✅ 12+ 種專業節點類型庫
✅ 拖拽式節點創建和連接
✅ 基本節點屬性面板
✅ 畫布工具欄（縮放、對齊、佈局）

---

## 🖥️ Desktop 版本佈局（≥1280px）

### 完整 3-Column Layout

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│ ← Back   [🔀 Workflow Editor] [客戶服務流程 v2.3]          [💾 儲存] [▶️ 執行] ⚙️ 🔔 👤 Alex      │
├──────────────┬────────────────────────────────────────────────────────────┬────────────────────────┤
│              │                                                            │                        │
│ 🧩 節點庫    │  🎨 無限畫布編輯區域                                       │  ⚙️ 屬性與設定         │
│              │                                                            │                        │
│ 🔍 搜尋      │  ┌─────────────────────────────────────────────────────┐  │  ┌──────────────────┐ │
│ ┌──────────┐│  │  🔧 工具欄                                          │  │  │ 節點: User Input │ │
│ │搜尋節點...││  │  [🖱️ 選擇] [➕ 新增] [🔗 連接] [📐 對齊] [🔍-/+] │  │  │ ID: node_001     │ │
│ └──────────┘│  └─────────────────────────────────────────────────────┘  │  └──────────────────┘ │
│              │                                                            │                        │
│ 📂 基礎節點  │            ┌─────────────┐                                │  📝 基本設定           │
│ ├─ 🎬 開始   │            │  🎬 Start   │                                │  ┌──────────────────┐ │
│ ├─ 🏁 結束   │            │   觸發器    │                                │  │ 節點名稱:        │ │
│ ├─ 💬 輸入   │            └──────┬──────┘                                │  │ User Input       │ │
│ ├─ 📊 輸出   │                   │                                       │  ├──────────────────┤ │
│ └─ ⏸️ 等待   │                   ▼                                       │  │ 描述:            │ │
│              │            ┌─────────────┐                                │  │ 收集用戶問題輸入  │ │
│ 🤖 Agent     │            │  💬 Input   │ ◄─────────┐                   │  ├──────────────────┤ │
│ ├─ 🎯 單一   │            │  用戶問題   │           │                   │  │ 輸入類型:        │ │
│ ├─ 👥 多重   │            └──────┬──────┘           │                   │  │ ☑ 文字           │ │
│ ├─ 🔄 循環   │                   │                  │                   │  │ ☐ 文件           │ │
│ └─ 🌊 串流   │                   ▼                  │                   │  │ ☐ 圖片           │ │
│              │            ┌─────────────┐           │                   │  └──────────────────┘ │
│ 🔀 邏輯控制  │         ┌─>│ 🎯 Agent_1  │─────┐    │                   │                        │
│ ├─ ❓ 條件   │         │  │  分類專家   │     │    │                   │  🔗 連接設定           │
│ ├─ 🔁 循環   │         │  └─────────────┘     │    │                   │  ┌──────────────────┐ │
│ ├─ 🔀 分支   │  ┌──────┴──────┐              │    │                   │  │ 輸入來源:        │ │
│ ├─ 🔗 合併   │  │ 🔀 Router   │              ▼    │                   │  │ ● Manual         │ │
│ └─ ⏱️ 計時器 │  │  路由分發   │       ┌─────────────┐                  │  │ ○ Previous Node  │ │
│              │  └──────┬──────┘       │ 🎯 Agent_2  │                  │  └──────────────────┘ │
│ 📊 數據處理  │         │              │  處理專家   │                  │                        │
│ ├─ 🔄 轉換   │         │              └──────┬──────┘                  │  ✅ 驗證規則           │
│ ├─ 🔍 查詢   │         │                     │                         │  ┌──────────────────┐ │
│ ├─ 📝 格式化 │         │                     ▼                         │  │ ☑ 必填           │ │
│ ├─ 🧮 計算   │         └─────────────> ┌─────────────┐                 │  │ ☑ 最小長度: 5    │ │
│ └─ 📂 儲存   │                         │  📊 Output  │                 │  │ ☐ 最大長度       │ │
│              │                         │  結果輸出   │                 │  └──────────────────┘ │
│ 🔌 整合節點  │                         └─────────────┘                 │                        │
│ ├─ 🗄️ 資料庫 │                                                         │  [✅ 套用] [❌ 取消]   │
│ ├─ 🌐 API    │  畫布: 0% zoom | Grid: 20px | Nodes: 6                 │                        │
│ ├─ 📧 郵件   │                                                         │                        │
│ ├─ 📁 檔案   │                                                         │                        │
│ └─ 🔔 通知   │                                                         │                        │
│              │                                                         │                        │
│ 💼 進階功能  │                                                         │                        │
│ ├─ 🧠 記憶   │                                                         │                        │
│ ├─ 🔐 權限   │                                                         │                        │
│ ├─ 📊 分析   │                                                         │                        │
│ └─ 🧪 測試   │                                                         │                        │
│              │                                                         │                        │
│ [260px]      │              [主要畫布區域 - 無限滾動]                  │  [380px]               │
└──────────────┴─────────────────────────────────────────────────────────┴────────────────────────┘
```

---

## 📊 內容層級（Desktop - Part 1）

### Level 1: 頁面結構（3-Column Layout）

```
┌───────────────┬──────────────────┬────────────────┐
│ 左側節點庫欄  │ 中央畫布編輯區   │ 右側屬性面板   │
│ (260px)       │ (主要內容)       │ (380px)        │
│               │                  │                │
│ - 節點搜尋    │ - 畫布工具欄     │ - 節點屬性     │
│ - 節點分類    │ - 無限畫布區域   │ - 連接設定     │
│ - 拖拽創建    │ - 節點連接線     │ - 驗證規則     │
│               │ - 縮放控制       │ - 執行日誌     │
└───────────────┴──────────────────┴────────────────┘
```

#### 左側節點庫欄（260px）
**優先級**: P0（核心功能）
**用戶角色**: All（不同角色看到不同推薦）

**功能元素**:
- 🔍 **節點搜尋框**: 快速過濾節點（支持名稱、標籤、描述）
- 📂 **分類樹狀結構**:
  - 基礎節點（Start, End, Input, Output, Wait）
  - Agent 節點（單一、多重、循環、串流）
  - 邏輯控制（條件、循環、分支、合併、計時器）
  - 數據處理（轉換、查詢、格式化、計算、儲存）
  - 整合節點（資料庫、API、郵件、檔案、通知）
  - 進階功能（記憶、權限、分析、測試）

**互動方式**:
- 拖拽節點到畫布創建
- 雙擊節點快速添加到畫布中心
- 右鍵顯示節點詳細資訊
- Hover 顯示節點簡介和連接要求

**角色化視圖**:
```yaml
Alex (IT 開發者):
  - 顯示所有節點類型
  - 推薦: 進階功能、整合節點
  - 快捷鍵提示顯著

Sarah (業務分析師):
  - 隱藏技術複雜節點
  - 推薦: 基礎節點、數據處理
  - 模板快捷入口

David (企業管理員):
  - 權限管理節點顯著
  - 推薦: 審批、通知節點
  - 合規性檢查提示
```

#### 中央畫布編輯區（主要內容）
**優先級**: P0（核心功能）
**用戶角色**: All

**畫布工具欄（頂部）**:
```
┌─────────────────────────────────────────────────────────┐
│ [🖱️ 選擇] [➕ 新增] [🔗 連接] [✂️ 剪下] [📋 複製]      │
│ [🗑️ 刪除] [↩️ 復原] [↪️ 重做] [📐 對齊] [🎨 佈局]      │
│ [🔍 25%][50%][100%][150%][200%][🔍+] 網格: [✓] 20px     │
└─────────────────────────────────────────────────────────┘
```

**畫布功能**:
1. **無限畫布**
   - 可縮放: 25% - 500%
   - 可平移: 拖拽背景移動
   - 網格系統: 20px 網格對齊（可切換）
   - 迷你地圖: 右下角顯示完整流程概覽

2. **節點操作**
   - 拖拽移動節點
   - 多選（Shift + 點擊、框選）
   - 群組選擇（Ctrl + G）
   - 對齊和分佈工具

3. **連接線**
   - 拖拽節點輸出點到輸入點創建連接
   - 自動路由（避開節點）
   - 連接類型: 成功（綠色）、失敗（紅色）、條件（黃色）
   - 懸浮顯示數據流預覽

4. **視覺反饋**
   - 拖拽時顯示可連接的輸入/輸出點（高亮）
   - 不兼容連接顯示紅色禁止圖標
   - 選中節點顯示藍色邊框
   - 執行中節點顯示動畫脈衝

**快捷鍵**:
```
Space + 拖拽        → 平移畫布
Ctrl + 滾輪         → 縮放
Ctrl + A            → 全選節點
Ctrl + C/V/X        → 複製/貼上/剪下
Delete              → 刪除選中節點
Ctrl + Z/Y          → 復原/重做
Ctrl + G            → 群組選擇
Ctrl + Shift + A    → 自動排列
```

#### 右側屬性面板（380px）
**優先級**: P0（核心功能）
**用戶角色**: All

**面板結構**:
```
┌────────────────────────────┐
│ 節點: [節點類型圖標 + 名稱] │
│ ID: node_xxx                │
├────────────────────────────┤
│ 📑 標籤頁:                  │
│ [📝 基本] [🔗 連接] [⚙️ 進階]│
├────────────────────────────┤
│                            │
│ [動態屬性表單內容]          │
│                            │
│ - 節點名稱                  │
│ - 描述                      │
│ - 類型特定配置              │
│ - 輸入/輸出設定             │
│ - 驗證規則                  │
│                            │
├────────────────────────────┤
│ [✅ 套用] [❌ 取消] [🔄 重置]│
└────────────────────────────┘
```

**基本標籤**:
- 節點名稱（必填）
- 節點描述
- 節點圖標選擇
- 顏色標籤（用於分類和視覺化）

**連接標籤**:
- 輸入來源配置
- 輸出目標配置
- 數據映射規則
- 連接驗證

**進階標籤**:
- 執行超時設定
- 重試策略
- 錯誤處理
- 日誌級別

---

### Level 2: 節點類型庫（完整定義）

#### 🎬 基礎節點（5 種）

##### 1. Start Node（開始節點）
```yaml
類型: start
圖標: 🎬
顏色: #10B981 (綠色)
用途: 工作流程入口，定義觸發條件
輸入: 無
輸出: 1 個（觸發事件）

配置屬性:
  - 觸發類型:
      - Manual（手動觸發）
      - Schedule（定時觸發）
      - Webhook（API 觸發）
      - Event（事件觸發）
  - 觸發條件: [依類型動態顯示]
  - 輸入 Schema: JSON Schema 定義

連接規則:
  - 必須是流程的第一個節點
  - 只能有輸出連接，無輸入連接
  - 一個流程只能有一個 Start 節點

視覺樣式:
  ┌─────────────┐
  │  🎬 Start   │
  │   觸發器    │
  └──────┬──────┘
         ▼
```

##### 2. End Node（結束節點）
```yaml
類型: end
圖標: 🏁
顏色: #EF4444 (紅色)
用途: 工作流程終點，返回最終結果
輸入: 多個（接收任何節點）
輸出: 無

配置屬性:
  - 結果類型:
      - Success（成功結束）
      - Failure（失敗結束）
      - Cancel（取消結束）
  - 輸出格式: JSON/Text/File
  - 結果 Schema: JSON Schema 定義
  - 通知設定: [可選]發送完成通知

連接規則:
  - 必須是流程的最後節點
  - 只能有輸入連接，無輸出連接
  - 一個流程可以有多個 End 節點（不同結果路徑）

視覺樣式:
  ┌─────────────┐
  │  🏁 End     │
  │  成功結束   │
  └─────────────┘
```

##### 3. Input Node（輸入節點）
```yaml
類型: input
圖標: 💬
顏色: #3B82F6 (藍色)
用途: 收集用戶輸入或外部數據
輸入: 0-1 個（可選前置數據）
輸出: 1 個（用戶輸入結果）

配置屬性:
  - 輸入類型:
      - Text（文字輸入）
      - Number（數字輸入）
      - File（文件上傳）
      - Image（圖片上傳）
      - JSON（結構化數據）
      - Form（表單多欄位）
  - 驗證規則:
      - 必填 Required
      - 最小/最大長度
      - 格式驗證（Regex）
      - 自定義驗證函數
  - 預設值: 可選
  - 佔位符文字: 提示用戶

連接規則:
  - 通常連接到 Start 或其他節點後
  - 輸出可連接到任何節點

視覺樣式:
  ┌─────────────┐
  │  💬 Input   │
  │  用戶問題   │
  └──────┬──────┘
         ▼
```

##### 4. Output Node（輸出節點）
```yaml
類型: output
圖標: 📊
顏色: #8B5CF6 (紫色)
用途: 展示中間結果或最終輸出
輸入: 1 個（要輸出的數據）
輸出: 1 個（可選繼續流程）

配置屬性:
  - 輸出類型:
      - Display（頁面顯示）
      - Download（文件下載）
      - API Response（API 返回）
      - Notification（通知）
  - 輸出格式:
      - Text（純文字）
      - Markdown（格式化文字）
      - JSON（結構化數據）
      - CSV（表格數據）
      - Chart（圖表）
  - 顯示模板: 自定義輸出樣式

連接規則:
  - 必須有輸入連接
  - 輸出連接可選（中間輸出）

視覺樣式:
  ┌─────────────┐
  │  📊 Output  │
  │  結果展示   │
  └──────┬──────┘
         ▼ (可選)
```

##### 5. Wait Node（等待節點）
```yaml
類型: wait
圖標: ⏸️
顏色: #F59E0B (橙色)
用途: 暫停流程執行，等待時間或事件
輸入: 1 個
輸出: 1 個

配置屬性:
  - 等待類型:
      - Duration（固定時間）
      - Until（等到特定時間點）
      - Event（等待外部事件）
      - Approval（等待人工審批）
  - 等待條件:
      - 時間: 秒/分/小時/天
      - 時間點: ISO 8601 時間戳
      - 事件名稱: 自定義事件
      - 審批者: 用戶/角色
  - 超時設定: 最長等待時間

連接規則:
  - 必須有輸入和輸出連接
  - 可用於限流、延遲、審批流程

視覺樣式:
  ┌─────────────┐
  │  ⏸️ Wait    │
  │  等待 5 秒  │
  └──────┬──────┘
         ▼
```

---

#### 🤖 Agent 節點（4 種）

##### 6. Single Agent Node（單一 Agent）
```yaml
類型: agent_single
圖標: 🎯
顏色: #06B6D4 (青色)
用途: 執行單一 Agent 任務
輸入: 1 個（任務輸入）
輸出: 1 個（執行結果）

配置屬性:
  - Agent 選擇: [下拉選單]從已創建的 Agent 列表選擇
  - 輸入映射:
      - 提示詞模板
      - 變量綁定（來自前置節點）
  - 執行設定:
      - Temperature: 0.0 - 1.0
      - Max Tokens: 數量限制
      - Stream: 是否串流輸出
  - 超時: 最大執行時間
  - 重試: 失敗重試次數

連接規則:
  - 必須選擇一個有效的 Agent
  - 輸入必須匹配 Agent 的輸入 Schema
  - 輸出結構由 Agent 定義

視覺樣式:
  ┌─────────────┐
  │  🎯 Agent   │
  │  分類專家   │
  │  Model: GPT4│
  └──────┬──────┘
         ▼
```

##### 7. Multi Agent Node（多重 Agent）
```yaml
類型: agent_multi
圖標: 👥
顏色: #EC4899 (粉紅色)
用途: 並行執行多個 Agent，彙總結果
輸入: 1 個（共同任務輸入）
輸出: 1 個（合併結果）

配置屬性:
  - Agent 列表: [多選]選擇 2-10 個 Agent
  - 執行策略:
      - Parallel（並行執行所有）
      - Sequential（順序執行）
      - Race（競速，取最快）
  - 結果合併:
      - Concatenate（串接）
      - Vote（投票取多數）
      - Average（平均）
      - Custom Function（自定義）
  - 失敗處理:
      - All Success（必須全部成功）
      - Partial Success（部分成功即可）
      - Best Effort（盡力執行）

連接規則:
  - 至少選擇 2 個 Agent
  - 適用於需要多視角分析的場景

視覺樣式:
  ┌─────────────┐
  │  👥 Multi   │
  │  3 Agents   │
  │  並行執行   │
  └──────┬──────┘
         ▼
```

##### 8. Loop Agent Node（循環 Agent）
```yaml
類型: agent_loop
圖標: 🔄
顏色: #14B8A6 (teal)
用途: 循環執行 Agent 直到條件滿足
輸入: 1 個（初始輸入）
輸出: 1 個（最終結果）

配置屬性:
  - Agent 選擇: 單一 Agent
  - 循環條件:
      - Max Iterations（最大次數）
      - Until Condition（條件滿足）
      - Quality Threshold（品質閾值）
  - 迭代策略:
      - Each Time New Input（每次新輸入）
      - Refine Previous（改進前次結果）
      - Accumulate（累積結果）
  - 循環變量: 可在每次迭代中使用的變量
  - 終止條件: JavaScript 表達式

連接規則:
  - 適用於需要迭代改進的場景
  - 注意避免無限循環（設置最大次數）

視覺樣式:
  ┌─────────────┐
  │  🔄 Loop    │
  │  優化 Agent │
  │  最多 5 次  │
  └──────┬──────┘
         ▼
```

##### 9. Stream Agent Node（串流 Agent）
```yaml
類型: agent_stream
圖標: 🌊
顏色: #0EA5E9 (天藍色)
用途: 串流輸出 Agent 結果（適合長文本）
輸入: 1 個
輸出: 1 個（串流）

配置屬性:
  - Agent 選擇: 支持串流的 Agent
  - 串流設定:
      - Chunk Size（塊大小）
      - Flush Interval（刷新間隔）
      - Buffer Size（緩衝大小）
  - 顯示設定:
      - Real-time Display（實時顯示）
      - Progressive Enhancement（逐步增強）
  - 完成條件:
      - 自動檢測結束標記
      - 超時終止

連接規則:
  - 需要下游節點支持串流輸入
  - 適合長時間執行的生成任務

視覺樣式:
  ┌─────────────┐
  │  🌊 Stream  │
  │  生成 Agent │
  │  實時輸出   │
  └──────┬──────┘
         ▼
```

---

#### 🔀 邏輯控制節點（5 種）

##### 10. Condition Node（條件節點）
```yaml
類型: condition
圖標: ❓
顏色: #FBBF24 (黃色)
用途: 根據條件分支執行路徑
輸入: 1 個（判斷數據）
輸出: 2-5 個（分支路徑）

配置屬性:
  - 條件類型:
      - Simple（簡單比較）
      - Expression（JavaScript 表達式）
      - AI Decision（AI 判斷）
  - 分支定義:
      - If Branch（條件為真）
      - Else Branch（條件為假）
      - Else If Branches（多個條件）
  - 判斷邏輯:
      - 比較運算符: ==, !=, >, <, >=, <=
      - 邏輯運算符: AND, OR, NOT
      - 模式匹配: Regex

連接規則:
  - 必須有至少 2 個輸出分支
  - 每個分支必須有明確的條件標籤

視覺樣式:
        ┌──────┐
        │  ❓  │
        │ If?  │
        └─┬─┬─┘
    True │ │ False
          ▼ ▼
```

##### 11. Loop Node（循環節點）
```yaml
類型: loop
圖標: 🔁
顏色: #A78BFA (淡紫色)
用途: 對數組或列表的每個元素執行操作
輸入: 1 個（數組數據）
輸出: 2 個（迭代輸出 + 完成輸出）

配置屬性:
  - 循環類型:
      - For Each（遍歷每個元素）
      - While（條件循環）
      - Do While（至少執行一次）
  - 迭代變量:
      - Item（當前元素）
      - Index（當前索引）
      - Array（完整數組）
  - 循環控制:
      - Max Iterations（最大次數）
      - Break Condition（中斷條件）
      - Continue Condition（跳過條件）
  - 並行執行: 是否並行處理元素

連接規則:
  - 迭代輸出連接到循環體內的節點
  - 循環體最後節點必須連回 Loop 節點
  - 完成輸出連接到循環後的節點

視覺樣式:
  ┌─────────────┐
  │  🔁 Loop    │
  │  For Each   │
  └──┬───────┬──┘
     │ iter  │ done
     ▼       ▼
```

##### 12. Router Node（路由分發節點）
```yaml
類型: router
圖標: 🔀
顏色: #F97316 (橙色)
用途: 根據規則將數據路由到不同的處理節點
輸入: 1 個
輸出: 2-10 個（路由目標）

配置屬性:
  - 路由策略:
      - Rule-Based（規則路由）
      - Load Balance（負載均衡）
      - Priority（優先級路由）
      - Random（隨機路由）
  - 路由規則:
      - 每個輸出定義匹配條件
      - 優先級排序
      - 預設路由（Fallback）
  - 路由選項:
      - Exclusive（互斥，只走一條）
      - Broadcast（廣播，走全部）

連接規則:
  - 至少 2 個輸出分支
  - 必須定義預設路由以避免數據丟失

視覺樣式:
     ┌──────────┐
     │  🔀 Route│
     └─┬──┬──┬─┘
       ▼  ▼  ▼
      1   2  3
```

##### 13. Merge Node（合併節點）
```yaml
類型: merge
圖標: 🔗
顏色: #84CC16 (萊姆綠)
用途: 合併多個分支的數據
輸入: 2-10 個（多個分支）
輸出: 1 個（合併結果）

配置屬性:
  - 合併策略:
      - Wait All（等待全部）
      - Wait Any（任意一個）
      - Wait First N（等待前 N 個）
  - 合併方式:
      - Array（組成數組）
      - Object（組成對象）
      - Concatenate（串接）
      - Custom（自定義函數）
  - 超時設定:
      - 等待超時時間
      - 超時處理（使用已有數據/失敗）

連接規則:
  - 至少 2 個輸入連接
  - 輸出單一合併結果

視覺樣式:
       1  2  3
       ▼  ▼  ▼
     ┌──┴──┴──┐
     │  🔗 Merge│
     └────┬────┘
          ▼
```

##### 14. Timer Node（計時器節點）
```yaml
類型: timer
圖標: ⏱️
顏色: #EF4444 (紅色)
用途: 定時觸發或測量執行時間
輸入: 0-1 個（可選）
輸出: 1 個（觸發信號）

配置屬性:
  - 計時器類型:
      - Interval（間隔觸發）
      - Cron（定時任務）
      - Stopwatch（計時測量）
  - 觸發設定:
      - Interval: 每 N 秒/分/小時
      - Cron: Cron 表達式
      - Repeat: 重複次數
  - 時區設定: UTC/Local

連接規則:
  - 可作為流程起點（無輸入）
  - 也可接收輸入作為計時開始信號

視覺樣式:
  ┌─────────────┐
  │  ⏱️ Timer   │
  │  每 5 分鐘  │
  └──────┬──────┘
         ▼
```

---

## 🎨 視覺設計規範（Part 1）

### 節點視覺標準

#### 標準節點尺寸
```yaml
Default:
  寬度: 180px
  高度: 80px
  圓角: 8px
  邊框: 2px solid

Large (Agent 節點):
  寬度: 200px
  高度: 100px
  圓角: 8px
  邊框: 2px solid

Small (基礎節點):
  寬度: 160px
  高度: 70px
  圓角: 8px
  邊框: 2px solid
```

#### 節點狀態顏色
```yaml
Normal (正常):
  邊框: #E5E7EB (灰色)
  背景: #FFFFFF (白色)

Selected (選中):
  邊框: #3B82F6 (藍色)
  背景: #EFF6FF (淡藍色)
  陰影: 0 0 0 3px rgba(59, 130, 246, 0.3)

Running (執行中):
  邊框: #10B981 (綠色 - 動畫脈衝)
  背景: #ECFDF5 (淡綠色)

Error (錯誤):
  邊框: #EF4444 (紅色)
  背景: #FEF2F2 (淡紅色)

Disabled (停用):
  邊框: #D1D5DB (淺灰色)
  背景: #F9FAFB (灰白色)
  透明度: 0.6
```

#### 連接線樣式
```yaml
Default (預設):
  顏色: #9CA3AF (中灰色)
  寬度: 2px
  類型: 實線

Success (成功路徑):
  顏色: #10B981 (綠色)
  寬度: 2px
  類型: 實線

Error (錯誤路徑):
  顏色: #EF4444 (紅色)
  寬度: 2px
  類型: 實線

Condition (條件路徑):
  顏色: #F59E0B (橙色)
  寬度: 2px
  類型: 虛線

Active (執行中):
  顏色: #3B82F6 (藍色)
  寬度: 3px
  類型: 實線 + 動畫流動
```

---

## 🛠️ 技術實現提示（Part 1）

### Canvas 引擎選擇
```typescript
// 推薦技術棧
Canvas Library: React Flow / xyflow
  - 理由:
    - 完整的節點編輯功能
    - 良好的 React 整合
    - 豐富的自定義能力
    - 活躍的社區支持

Alternative:
  - D3.js + React (更靈活但需要更多開發)
  - Konva.js (高性能但較底層)
  - Cytoscape.js (圖形可視化專用)
```

### 狀態管理架構
```typescript
// Zustand Store 結構
interface WorkflowEditorStore {
  // 畫布狀態
  canvas: {
    zoom: number;
    position: { x: number; y: number };
    gridEnabled: boolean;
    snapToGrid: boolean;
  };

  // 節點狀態
  nodes: Node[];
  selectedNodes: string[];

  // 連接線狀態
  edges: Edge[];

  // 操作歷史
  history: {
    past: Snapshot[];
    future: Snapshot[];
  };

  // UI 狀態
  ui: {
    activePanel: 'node-library' | 'properties' | 'execution';
    selectedNodeId: string | null;
  };

  // Actions
  addNode: (node: Node) => void;
  updateNode: (id: string, updates: Partial<Node>) => void;
  deleteNode: (id: string) => void;
  addEdge: (edge: Edge) => void;
  undo: () => void;
  redo: () => void;
}
```

### 節點數據結構
```typescript
// 節點基礎介面
interface BaseNode {
  id: string;
  type: NodeType;
  position: { x: number; y: number };
  data: {
    label: string;
    description?: string;
    icon: string;
    color: string;
    config: Record<string, any>;
  };
  inputs: Port[];
  outputs: Port[];
  status: 'idle' | 'running' | 'success' | 'error';
  validation: ValidationResult;
}

// 連接埠定義
interface Port {
  id: string;
  name: string;
  type: 'input' | 'output';
  dataType: string; // 'any' | 'string' | 'number' | 'object' | 'array'
  required: boolean;
  connected: boolean;
}

// 連接線定義
interface Edge {
  id: string;
  source: string; // 源節點 ID
  sourceHandle: string; // 源埠 ID
  target: string; // 目標節點 ID
  targetHandle: string; // 目標埠 ID
  type: 'default' | 'success' | 'error' | 'condition';
  animated: boolean;
  label?: string;
}
```

---

## ✅ Part 1 檢查清單

### 完成項目
- [✅] 頁面概覽和目標定義
- [✅] 3-Column Desktop 佈局設計
- [✅] 完整節點類型庫定義（14 種節點）
  - [✅] 基礎節點 (5)
  - [✅] Agent 節點 (4)
  - [✅] 邏輯控制節點 (5)
- [✅] 視覺設計規範（節點、連接線、狀態）
- [✅] 技術實現提示（Canvas 引擎、狀態管理、數據結構）

### 下一步（Part 2）
- [ ] 數據處理節點定義 (5 種)
- [ ] 整合節點定義 (5 種)
- [ ] 進階功能節點定義 (4 種)
- [ ] 完整屬性面板設計
- [ ] 節點配置表單系統
- [ ] 連接規則驗證系統

---

**文檔狀態**: Part 1/5 完成 ✅
**下一階段**: Part 2 - 節點配置系統
**預計完成**: 2025-10-29

